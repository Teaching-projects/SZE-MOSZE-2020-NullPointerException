name: Google test

on: push


jobs:
  build:
    runs-on: ubuntu-latest
    container: gradden/test:latest
    steps:
      - name: Build Enviroment
        run: cd /usr/src/gtest && cmake . && make && ln -s /usr/lib/libgtest.a /usr/local/lib/libgtest.a && ln -s /usr/lib/libgtest_main.a /usr/local/lib/libgtest_main.a
      - uses: actions/checkout@v2      
      - name: CmakeList
        run: cd test && cmake CMakeLists.txt && make
      - name: Permissions
        run:  chmod +x test/unit_test.cpp
      - run: g++-10 -std=c++17 *.cpp -lstdc++fs -o unit_test
      - name: Run test
        run: make gtest
